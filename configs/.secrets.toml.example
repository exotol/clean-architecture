[default]
# Put real secrets into `configs/.secrets.toml` (gitignored).
# This example file must contain NO real credentials.

# Example:
# OPENSEARCH.USER = "admin"
# OPENSEARCH.PASSWORD = "change-me"

